# Tools required by the Plan skill

tools:
  - name: knowledge_search
    description: Check what resources exist for the student's goal
    required: true

  - name: write_output
    description: Produce the structured plan
    required: true

  - name: read_file
    description: Read existing plans or context to avoid duplication
    required: false
